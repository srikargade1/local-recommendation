name: Deploy to Render

on:
  push:
    branches:
      - main

jobs:
  notify-render:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy Hooks
        run: |
          curl -X POST https://api.render.com/deploy/srv-d16tbmjuibrs73eongc0?key=aBE7Yy8j_Lc
          curl -X POST https://api.render.com/deploy/srv-d16tbmjuibrs73eongcg?key=Uc9j1_9HYU0
